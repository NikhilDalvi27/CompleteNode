git
1)untracked files
files not tracked by git

2)unstaged changes
changes to file which git is already tracking

3)staged changes
Things that will be included in
next commit

4)commits
all the list of commits




1)add command
untracked  and unstaged changes -> staged

2)commit command
staged -> commit


Git command sequence
1) git init -- to initialize a git repository (only once)
2) .gitignore -- list of thing u dont want to track (only once)
3)git add .     --- to move everything to staging area
4)git commit -m "commit message" - to commit




5)get the ssh keys from (git bash) IMP
 ls -a -l ~/.ssh
 if keys not present then u can create them using
 ssh-keygen command


do heroku login
